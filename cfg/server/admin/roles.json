{
    "$schema": "../../../schemas/server/admin_roles.json",

    "ent_commands": [
        "ent_trigger",
        "ent_triggerrange"
    ],

    "everyone_commands": [
        "ent_dumpinfo"
    ]
}
